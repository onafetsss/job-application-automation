# Continue Here — Job Application Automation

**Saved:** 2026-05-28  
**Phase:** 02 — Ingest, Generate & Email Apply  
**Status:** Phase 2 COMPLETE and DEPLOYED ✅

---

## What Was Accomplished This Session

Phase 2 is fully live on the VPS at `https://n8n.runlucid.co`:

### Infrastructure
- FastAPI backend (`stack-job-app-api-1`) running on VPS at `/opt/stack/` on `n8n-net` Docker network
- Accessible internally at `http://job-app-api:8000`
- Google token at `/opt/job-app/.google_token.json`
- Resumes uploaded to `/opt/job-app/resumes/` (4 PDFs)

### n8n Workflows (all active)
1. **Error Handler** — catches uncaught workflow errors → Telegram
2. **Heartbeat** — pings Telegram every 30 min
3. **Gmail Ingest** — polls Gmail every 1h, extracts jobs via Claude Haiku, ingests leads
4. **JobSpy Scrape** — scrapes Indeed every 4h (search term needs updating — see below)
5. **Kalibrr Scrape** — scrapes Kalibrr every 4h (search term needs updating — see below)
6. **AI Apply Pipeline** — every 15min, selects resume, generates cover letter, sends email

### Fixes Applied This Session
- SQLite busy timeout raised to 30s (`src/queue/db.py`)
- Claude Haiku model corrected to `claude-haiku-4-5-20251001` (`src/api/routes/resume.py`)
- ANTHROPIC_API_KEY explicitly passed to Anthropic client
- Gmail API enabled in Google Cloud Console
- `https://mail.google.com/` scope added to OAuth consent screen
- n8n attribution footer disabled via "Append n8n Attribution" toggle in Gmail node
- Extract Cover Letter strips leading `=` chars from Claude output
- Eligibility config updated to target BD/growth/sales roles

---

## Where We Stopped

User paused after confirming the AI Apply Pipeline works end-to-end. Last item discussed: **updating scraper search terms** from "Product Manager" to multiple BD/growth/sales terms.

---

## Immediate Next Steps

### 1. Update scraper search terms in n8n (10 min)
The JobSpy and Kalibrr workflows still have `search_term: "Product Manager"`. Need to either:
- Update to `Business Development Manager` as primary term
- **OR** duplicate workflows for each search term (recommended):
  - JobSpy × 3: "Business Development Manager", "Growth Marketing Manager", "Account Executive"
  - Kalibrr × 3: same terms
  
  Workflow JSON files are at `n8n/workflows/`. Offer to generate all 6 new JSONs.

### 2. Change API key from `changeme123` (5 min)
On VPS:
```bash
# Update in /opt/stack/.env
# Update JOB_APP_API_KEY=<new-secure-key>
# Then update FastAPI Key credential in n8n UI
# Then rebuild: docker compose -f /opt/stack/docker-compose.yml build job-app-api && up -d
```

### 3. Subscribe to LinkedIn job alerts
User needs to set up LinkedIn job alerts for target roles sent to `stefano.dsilva@gmail.com` — this feeds the Gmail Ingest → AI Apply pipeline automatically.

---

## Phase 3 Planning (Next Milestone)
**Goal:** LinkedIn Easy Apply + web form automation  
**Key tech:** Camoufox (stealth Firefox), Playwright  
**Prerequisite:** Verify Camoufox 0.4.x still best approach at planning time  
**Risk:** LinkedIn account ban — design with pause-on-detection  

---

## VPS Details
- IP: `187.127.100.109`
- Stack: `/opt/stack/`
- App code: `/opt/job-app/`
- n8n: `https://n8n.runlucid.co`
- DB: `/opt/job-app/data/jobs.db`
- Jobs in DB: 159+

## Key Credentials (n8n)
- `FastAPI Key` — Header Auth, X-API-Key: changeme123 ⚠️ change this
- `anthropicApi` — Header Auth, x-api-key: Anthropic key
- `Telegram Bot` — Telegram API
- `Gmail OAuth2` — Google OAuth2 with `https://mail.google.com/` scope

---

## Resume Instructions
```
/gsd-resume-work
```
Then say "Job Application Agent" and continue from Immediate Next Steps above.
