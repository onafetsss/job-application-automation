# Database
DB_PATH=data/jobs.db

# Eligibility config
ELIGIBILITY_CONFIG_PATH=config/eligibility.yaml

# Profile config (Phase 2+)
PROFILE_CONFIG_PATH=config/profile.yaml

# Telegram (Phase 2+)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Anthropic (Phase 2+)
ANTHROPIC_API_KEY=

# Gmail OAuth (Phase 2+)
GOOGLE_CREDENTIALS_PATH=.google_credentials.json
GOOGLE_TOKEN_PATH=.google_token.json

# Resume library (Phase 2+)
RESUMES_DIR=resumes

# n8n orchestration (Phase 2+)
# Generate via: openssl rand -hex 32
N8N_ENCRYPTION_KEY=
