---
context: phase
phase: 03-linkedin-easy-apply
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-05-29
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints were discovered this session. Execution was clean._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Selector drift | LinkedIn's Easy Apply button XPath is MEDIUM confidence — may drift between sessions | advisory | Always validate selector manually (Task 3 step 3) before activating the autonomous workflow |

<current_state>
Phase 03 is functionally complete. All 4 plans are built and tested:
- 03-01: camoufox dep + Docker browser support + SKIPPED state + test scaffolds ✓
- 03-02: LinkedInApplier browser module + screening shared function ✓
- 03-03: FastAPI /apply endpoints (linkedin-easy-apply, daily-count, queued-jobs) ✓
- 03-04: n8n workflow + gmail-ingest fix + session save script ✓ (Tasks 1 & 2)

**Blocked at: Plan 03-04, Task 3 — human checkpoint.**
The executor returned `bcf08e8` (SUMMARY.md committed noting Task 3 as paused).
The user needs to complete 5 manual steps, then type "approved".
</current_state>

<completed_work>
**Plan 03-01** (Wave 1) — COMPLETE — commit `5096a86`
- camoufox>=0.4.11 added to pyproject.toml + uv.lock
- docker/Dockerfile.api updated with xvfb + `python -m camoufox fetch`
- docker-compose.yml: 4 LINKEDIN_* env vars wired
- .env.example: LINKEDIN_* section + # SMARTPROXY_URL= placeholder
- JobStatus.SKIPPED + AuditEvent.SKIPPED added
- 5 test scaffolds: tests/browser/, tests/api/test_apply_routes.py, tests/unit/test_ingest_apply_type.py

**Plan 03-02** (Wave 2) — COMPLETE — commit `4c3f701`
- src/browser/linkedin_applier.py — LinkedInApplier class
  - Exceptions: ChallengeDetected, NoEasyApplyButton, UnknownFormField
  - Methods: check_for_challenge(), resolve_profile_field(), fill_form_fields(), _find_and_click_easy_apply(), _navigate_modal(), apply()
- src/preparation/screening.py — generate_screening_answers() shared function (extracted from FastAPI route)
- src/api/routes/application.py — refactored to call shared screening function (no self-HTTP)
- 3 browser tests GREEN

**Plan 03-03** (Wave 3) — COMPLETE — commit `d31bd7d`
- src/api/routes/apply/linkedin_apply.py — POST /apply/linkedin-easy-apply, GET /apply/daily-linkedin-count, GET /apply/queued-linkedin-jobs
- src/api/schemas.py — LinkedInApplyIn, LinkedInApplyOut
- src/api/app.py — /apply router registered
- 4 API tests GREEN

**Plan 03-04 Task 1** — COMPLETE — commit `d8d5661`
- n8n/workflows/gmail-ingest.json — gi-10 "Set Apply Type" node; apply_type expression-driven
- src/ingestion/gmail_client.py — resolve_apply_type(url) helper
- scripts/linkedin_session_save.py — AsyncCamoufox persistent session save
- tests/unit/test_ingest_apply_type.py — both tests GREEN (xpassed)

**Plan 03-04 Task 2** — COMPLETE — commit `c03eb77`
- n8n/workflows/linkedin-easy-apply.json — full workflow: Schedule → cap/window gate → GET queued → random Wait → POST apply → Telegram success/challenge routing
  - Daily cap enforced: LINKEDIN_DAILY_CAP (default 17)
  - Time window: LINKEDIN_APPLY_WINDOW_START/END (default 9-17)
  - Randomized wait: 8-25 min
  - Challenge terminates run (no retry per D-06)
  - active=false (safe to import)
</completed_work>

<remaining_work>
**Plan 03-04 Task 3** — BLOCKING HUMAN CHECKPOINT

The user must complete these steps, then type "approved" to resume:

1. **SESSION SAVE** — Run:
   ```
   LINKEDIN_PROFILE_DIR=data/linkedin_profile uv run python scripts/linkedin_session_save.py
   ```
   Browser opens to LinkedIn login. Log in manually (complete 2FA). Press Enter. Confirm `data/linkedin_profile/` is populated.

2. **FINGERPRINT** — With saved session, open a Camoufox browser → navigate to https://bot.sannysoft.com → confirm no red/failed rows.

3. **SELECTOR VALIDATION** — Open a LinkedIn job with Easy Apply → confirm XPath `//button[contains(@class, "jobs-apply-button")]` resolves. If drifted, type "failed: [description]" for selector correction.

4. **LIVE APPLY** — Trigger against one QUEUED linkedin job:
   ```
   curl -H "X-API-Key: <key>" \
        -H "Content-Type: application/json" \
        -d '{"job_id":"<uuid>"}' \
        http://localhost:8000/apply/linkedin-easy-apply
   ```
   Confirm: `{"status":"submitted"}`, DB row shows SUBMITTED, Telegram success notification arrives.

5. **WORKFLOW IMPORT** — Import `n8n/workflows/linkedin-easy-apply.json` into n8n UI. Verify fastApiKey + telegramBot credentials resolve. Leave `active: false` until ready for autonomous runs.

**After approval:**
- Mark 03-04 Task 3 complete (update SUMMARY.md to remove "paused" note)
- Run code review: `/gsd-code-review 03`
- Run phase verifier (gsd-verifier spawns automatically from execute-phase completion flow)
- Mark phase 03 complete in ROADMAP + STATE
</remaining_work>

<decisions_made>
- `resolve_apply_type` placed in `src/ingestion/gmail_client.py` — matches test file's candidate module list
- Telegram success node uses cross-node reference `$('Get Next LinkedIn Job').first().json.jobs[0].*` — POST /apply response only returns {status, job_id}, company/title/url must come from earlier node
- `storage_state` JSON export wrapped in try/except (non-fatal) — handles `__Host-` cookie serialization edge case (RESEARCH.md Pitfall 5)
- n8n Wait node uses `$('Check Cap + Window').first().json.waitSeconds` cross-node ref (explicit, safer than `$json.waitSeconds` through If nodes)
- Dockerfiles reorganized to `docker/` subdirectory; `docker-compose.yml` updated to `docker/Dockerfile.api`
- CLAUDE.md architecture section fully documented with folder tree + data flow diagram
</decisions_made>

<blockers>
- Plan 03-04 Task 3: blocking human checkpoint — requires live browser + real LinkedIn session + real job submission + n8n import. Cannot be automated.
- Pre-existing uncommitted files (not related to phase 03): `n8n/workflows/jobspy-scrape.json`, `n8n/workflows/kalibrr-scrape.json` — handle separately if needed.
</blockers>

## Required Reading (in order)

1. `.planning/phases/03-linkedin-easy-apply/03-04-SUMMARY.md` — full state of what was built, human checkpoint steps, key decisions
2. `.planning/phases/03-linkedin-easy-apply/03-CONTEXT.md` — locked decisions (D-02 apply_type, D-06 no-retry on challenge)
3. `./CLAUDE.md` — updated folder map (docker/ reorganization done this session)

## Infrastructure State

- **FastAPI API**: not running (start with `uv run uvicorn src.api.app:app --reload` or `docker compose up api`)
- **n8n**: not running (start with `docker compose up n8n`)
- **LinkedIn session**: NOT yet saved — this is the first item in the human checkpoint
- **n8n workflow**: NOT yet imported — step 5 of human checkpoint
- **Camoufox binary**: installed in Docker image (`docker/Dockerfile.api`); also fetchable locally via `python -m camoufox fetch`

<context>
We executed all 4 waves of Phase 03 in sequence this session. The phase is functionally complete — all auto code is written, tested, and committed. The only gate remaining is Task 3 of Plan 03-04: the user needs to run the session save script (opens a real browser), verify fingerprint, fire one real application, and import the n8n workflow. Once they type "approved", the continuation executor will close the plan and the phase will flow into code review + verification.
</context>

<next_action>
User types "approved" (or "failed: [description]" if selector drifted) → spawn continuation executor for 03-04 Task 3 → mark SUMMARY.md complete → run /gsd-code-review 03 → gsd-verifier creates VERIFICATION.md → phase.complete marks ROADMAP + STATE done → offer Phase 04.
</next_action>
