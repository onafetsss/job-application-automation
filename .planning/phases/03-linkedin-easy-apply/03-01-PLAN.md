---
phase: 03-linkedin-easy-apply
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - Dockerfile.api
  - docker-compose.yml
  - .env.example
  - src/queue/models.py
  - src/audit_log.py
  - tests/browser/__init__.py
  - tests/browser/test_linkedin_applier.py
  - tests/api/test_apply_routes.py
  - tests/unit/test_ingest_apply_type.py
autonomous: false
requirements: [APPLY-01]
user_setup:
  - service: camoufox
    why: "Stealth Firefox browser binary required for LinkedIn automation; ~100MB fetched at Docker build"
    dashboard_config:
      - task: "Verify camoufox package legitimacy on PyPI before install (blocking checkpoint in Task 1)"
        location: "https://pypi.org/project/camoufox/"

must_haves:
  truths:
    - "The project declares camoufox as a dependency and the Docker image can launch a Camoufox virtual-display session without FileNotFoundError"
    - "A job can be marked SKIPPED (distinct from FAILED) when it has no Easy Apply button or an unknown form field"
    - "LinkedIn timing/cap env vars resolve to defaults (window 9-17, cap 17) when unset"
    - "Browser and apply-route test scaffolds exist and run (RED state allowed) so later waves have Nyquist coverage"
  artifacts:
    - path: "pyproject.toml"
      provides: "camoufox dependency declaration"
      contains: "camoufox"
    - path: "Dockerfile.api"
      provides: "xvfb apt install + camoufox binary fetch"
      contains: "camoufox fetch"
    - path: "src/queue/models.py"
      provides: "JobStatus.SKIPPED enum member"
      contains: "SKIPPED"
    - path: "tests/browser/test_linkedin_applier.py"
      provides: "challenge detection / no-button / unknown-field unit tests"
      contains: "test_challenge_detected"
  key_links:
    - from: "Dockerfile.api"
      to: "camoufox binary"
      via: "RUN python -m camoufox fetch"
      pattern: "camoufox fetch"
    - from: "docker-compose.yml"
      to: "src/api FastAPI service"
      via: "LINKEDIN_* environment variables"
      pattern: "LINKEDIN_(PROFILE_DIR|APPLY_WINDOW_START|APPLY_WINDOW_END|DAILY_CAP)"
---

<objective>
Establish the LinkedIn Easy Apply foundation: add the Camoufox dependency and Firefox binary to the Docker image, add the `SKIPPED` job state, wire the LinkedIn timing/cap environment variables, and lay down the test scaffolds the rest of the phase will fill in.

Purpose: Every later wave (browser module, FastAPI route, n8n workflow) depends on these enabling artifacts. Without the Camoufox binary in Docker and the `SKIPPED` enum, the apply path cannot run or record its expected outcomes.

Output: Updated `pyproject.toml`, `Dockerfile.api`, `docker-compose.yml`, `.env.example`, `src/queue/models.py`, `src/audit_log.py`, and three RED test scaffolds under `tests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-linkedin-easy-apply/03-CONTEXT.md
@.planning/phases/03-linkedin-easy-apply/03-RESEARCH.md
@.planning/phases/03-linkedin-easy-apply/03-PATTERNS.md

<interfaces>
<!-- Current JobStatus enum (src/queue/models.py lines 13-19) — append SKIPPED after FAILED -->
From src/queue/models.py:
```python
class JobStatus(str, Enum):
    DISCOVERED = "DISCOVERED"
    QUEUED = "QUEUED"
    REJECTED = "REJECTED"
    APPLYING = "APPLYING"
    SUBMITTED = "SUBMITTED"
    FAILED = "FAILED"
```

<!-- Current AuditEvent enum (src/audit_log.py lines 13-24) — append SKIPPED -->
From src/audit_log.py:
```python
class AuditEvent(StrEnum):
    ...
    SUBMITTED = "SUBMITTED"
    FAILED = "FAILED"
    NOTIFIED = "NOTIFIED"
```

<!-- Current Dockerfile.api (8 lines) — insert xvfb + camoufox fetch after `uv sync`, before COPY src/ -->
From Dockerfile.api:
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml uv.lock ./
RUN pip install uv && uv sync --no-dev
COPY src/ src/
COPY main.py .
EXPOSE 8000
CMD ["uv", "run", "uvicorn", "src.api.app:app", "--host", "0.0.0.0", "--port", "8000"]
```

<!-- docker-compose.yml: api service env block lines 24-30, data volume mapped ./data:/data line 31 -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify camoufox package legitimacy (blocking gate, then add dependency)</name>
  <what-built>The researcher tagged `camoufox` as `[ASSUMED]` (slopcheck unavailable at research time). Per the package legitimacy gate, a human must confirm the package is genuine before it is installed. This checkpoint precedes the `uv add` install.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/camoufox/ — confirm latest version is 0.4.x (research found 0.4.11), release history goes back ~2 years, and the project links to https://github.com/daijro/camoufox.
    2. Open https://github.com/daijro/camoufox — confirm it is the anti-detect Firefox fork (stars, active commits, documented homepage camoufox.com).
    3. Confirm the package name is exactly `camoufox` (no typo-squat variant).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `uv add camoufox`, or describe the discrepancy to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add camoufox dependency and Docker browser support</name>
  <read_first>
    - pyproject.toml (current dependencies array, lines 1-26 — deps use "package>=version" format)
    - Dockerfile.api (current 8-line file)
    - docker-compose.yml (api service environment block lines 24-30; data volume ./data:/data line 31)
    - .env.example (current env var declarations)
    - .planning/phases/03-linkedin-easy-apply/03-RESEARCH.md (Standard Stack + Dockerfile.api addition + Runtime State Inventory env vars)
  </read_first>
  <action>
    Run `uv add camoufox` (only after Task 1 approval) so `camoufox>=0.4.11` is added to the `dependencies` array in pyproject.toml and uv.lock is updated. In `Dockerfile.api`, insert two RUN steps immediately after the `RUN pip install uv && uv sync --no-dev` line and before `COPY src/ src/`: (a) `RUN apt-get update && apt-get install -y xvfb --no-install-recommends && rm -rf /var/lib/apt/lists/*` to enable `headless="virtual"`, and (b) `RUN python -m camoufox fetch` to download the Firefox binary into the image layer. In `docker-compose.yml`, add to the `api` service `environment` block: `LINKEDIN_PROFILE_DIR=/data/linkedin_profile`, `LINKEDIN_APPLY_WINDOW_START=${LINKEDIN_APPLY_WINDOW_START:-9}`, `LINKEDIN_APPLY_WINDOW_END=${LINKEDIN_APPLY_WINDOW_END:-17}`, `LINKEDIN_DAILY_CAP=${LINKEDIN_DAILY_CAP:-17}`. The `data/` volume is already mounted at `./data:/data` — no new volume entry needed. In `.env.example`, add the four `LINKEDIN_*` variables with their default values plus a commented placeholder line for a future residential proxy (`# SMARTPROXY_URL=` per CONTEXT.md deferred ideas — placeholder only, not wired).
  </action>
  <verify>
    <automated>grep -v '^#' pyproject.toml | grep -c camoufox | grep -qv '^0$' && grep -c "camoufox fetch" Dockerfile.api | grep -qv '^0$' && grep -c "xvfb" Dockerfile.api | grep -qv '^0$' && grep -Eqc "LINKEDIN_PROFILE_DIR|LINKEDIN_DAILY_CAP" docker-compose.yml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml `dependencies` array contains an entry starting with `camoufox` (version >= 0.4.11)
    - Dockerfile.api contains the line `RUN apt-get update && apt-get install -y xvfb` (xvfb install) and a line containing `python -m camoufox fetch`, both positioned after `uv sync` and before `COPY src/ src/`
    - docker-compose.yml api service environment block contains `LINKEDIN_PROFILE_DIR=/data/linkedin_profile`, `LINKEDIN_APPLY_WINDOW_START`, `LINKEDIN_APPLY_WINDOW_END`, and `LINKEDIN_DAILY_CAP`
    - .env.example contains all four `LINKEDIN_*` variables and a commented `# SMARTPROXY_URL=` placeholder
    - `uv.lock` updated (git diff shows camoufox entry)
  </acceptance_criteria>
  <done>camoufox is a declared dependency, the Docker image installs xvfb and fetches the Camoufox Firefox binary, and the four LINKEDIN_* env vars are wired into compose and documented in .env.example.</done>
</task>

<task type="auto">
  <name>Task 3: Add SKIPPED job state, SKIPPED audit event, and RED test scaffolds</name>
  <read_first>
    - src/queue/models.py (JobStatus enum lines 13-19; status column line 38 is Column(String) — no migration needed)
    - src/audit_log.py (AuditEvent StrEnum lines 13-24; write_audit signature lines 39-70)
    - tests/unit/ and tests/integration/ (existing pytest-asyncio layout, asyncio_mode="auto" in pyproject.toml)
    - .planning/phases/03-linkedin-easy-apply/03-RESEARCH.md (Validation Architecture → Wave 0 Gaps + Phase Requirements → Test Map)
    - .planning/phases/03-linkedin-easy-apply/03-PATTERNS.md (JobStatus + AuditEvent modification sections)
  </read_first>
  <action>
    Add `SKIPPED = "SKIPPED"` to `JobStatus` in `src/queue/models.py` immediately after `FAILED = "FAILED"`. Add `SKIPPED = "SKIPPED"` to `AuditEvent` in `src/audit_log.py` immediately after `FAILED = "FAILED"`. No DB migration is required — the `status` column is `Column(String)`. Create `tests/browser/__init__.py` (empty package marker). Create `tests/browser/test_linkedin_applier.py` with three async test functions that import from `src.browser.linkedin_applier` (module created in Plan 02): `test_challenge_detected` (asserts a mocked page whose `.url` contains `/checkpoint/` causes the challenge check to return a non-None challenge string), `test_no_easy_apply_button` (asserts a mocked page where the Easy Apply locator `.count()` returns 0 raises `NoEasyApplyButton`), and `test_unknown_form_field` (asserts an unmappable field label raises `UnknownFormField`). Create `tests/api/__init__.py` if missing and `tests/api/test_apply_routes.py` with `test_daily_count` (DB fixture: insert two Application rows submitted today via the linkedin path, assert the daily-count helper returns 2) — the route/helper it targets is created in Plan 03. Create `tests/unit/test_ingest_apply_type.py` with `test_linkedin_url_apply_type` asserting that a URL containing `linkedin.com` resolves to `apply_type='linkedin_easy_apply'` (the resolution logic is added in Plan 04; this test may use a small pure helper or be marked xfail until then). These tests are allowed to be RED (import errors / xfail) at the end of this plan — they establish the Nyquist scaffold. Use `pytest.importorskip` or `pytest.mark.xfail(reason="implemented in Plan 0X")` so the suite does not hard-error on missing modules.
  </action>
  <verify>
    <automated>uv run python -c "from src.queue.models import JobStatus; from src.audit_log import AuditEvent; assert JobStatus.SKIPPED.value=='SKIPPED'; assert AuditEvent.SKIPPED.value=='SKIPPED'; print('PASS')" && uv run pytest tests/browser/ tests/unit/test_ingest_apply_type.py -q --co | grep -q test_ && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `from src.queue.models import JobStatus; JobStatus.SKIPPED.value == "SKIPPED"` evaluates true
    - `from src.audit_log import AuditEvent; AuditEvent.SKIPPED.value == "SKIPPED"` evaluates true
    - `tests/browser/test_linkedin_applier.py` defines `test_challenge_detected`, `test_no_easy_apply_button`, `test_unknown_form_field`
    - `tests/api/test_apply_routes.py` defines `test_daily_count`
    - `tests/unit/test_ingest_apply_type.py` defines `test_linkedin_url_apply_type`
    - `uv run pytest tests/ --co -q` collects without a hard collection error (xfail/importorskip used for not-yet-implemented modules)
  </acceptance_criteria>
  <done>JobStatus and AuditEvent both expose SKIPPED, and the three test files exist with the named test functions collectable by pytest (RED state acceptable).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI registry → build image | camoufox package + Firefox binary downloaded into the Docker image (supply chain) |
| host filesystem → Docker volume | `data/` (incl. future `linkedin_profile/`) mounted into the container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | `uv add camoufox` / `python -m camoufox fetch` | mitigate | Blocking human-verify checkpoint (Task 1) confirms package legitimacy on pypi.org + github.com/daijro/camoufox before install; camoufox is `[ASSUMED]` per RESEARCH.md |
| T-03-01 | Information Disclosure | `data/linkedin_profile/` session directory | mitigate | `.gitignore` already excludes `data/` (verified); session dir lives inside that ignored path — never committed |
| T-03-02 | Tampering | Camoufox Firefox binary in image | accept | Binary fetched from official camoufox distribution at build time; no runtime fetch; pinned camoufox version controls binary version |
</threat_model>

<verification>
- `uv run python -c "from src.queue.models import JobStatus; from src.audit_log import AuditEvent; print(JobStatus.SKIPPED, AuditEvent.SKIPPED)"` prints both SKIPPED values
- `docker compose config` parses without error and shows the four LINKEDIN_* env vars on the api service
- `uv run pytest tests/ --co -q` collects all test files including the new scaffolds
</verification>

<success_criteria>
- camoufox declared in pyproject.toml; Dockerfile.api installs xvfb and fetches the Camoufox binary
- JobStatus.SKIPPED and AuditEvent.SKIPPED exist
- Four LINKEDIN_* env vars wired in docker-compose.yml and documented in .env.example
- Test scaffolds for browser, apply routes, and ingest apply_type exist and collect (RED allowed)
</success_criteria>

<output>
Create `.planning/phases/03-linkedin-easy-apply/03-01-SUMMARY.md` when done
</output>
