---
phase: 03-linkedin-easy-apply
plan: 05
type: execute
wave: 1
depends_on: ["03-04"]
files_modified:
  - src/queue/models.py
  - src/audit_log.py
  - src/notify.py
  - src/browser/linkedin_applier.py
  - src/api/routes/apply/linkedin_apply.py
  - tests/unit/test_linkedin_sdui_fixes.py
autonomous: false
requirements: ["APPLY-01"]
must_haves:
  truths:
    - "Easy Apply trigger is located by visible text / aria-label containing 'Easy Apply' — class selectors are never used"
    - "Clicking the trigger waits for a confirmed modal selector to appear before declaring success, retrying up to 3 times"
    - "All modal fill/navigation calls are issued against the modal's own frame, not the top-level page frame"
    - "When the recaptcha enterprise frame is detected in the modal, the job is transitioned to NEEDS_HUMAN status and a Telegram alert is sent — no submission attempt is made"
    - "The /apply/linkedin-easy-apply endpoint returns {status: 'paused_human', job_id: ...} with HTTP 200 when reCAPTCHA pauses a job"
    - "NEEDS_HUMAN jobs are excluded from queued-linkedin-jobs and not retried autonomously"
    - "Unit tests for selector logic, reCAPTCHA detection predicate, and NEEDS_HUMAN state transition pass under pytest"
  artifacts:
    - path: "src/notify.py"
      provides: "async send_telegram(text) helper using httpx + TELEGRAM_BOT_TOKEN / TELEGRAM_CHAT_ID env vars"
      exports: ["send_telegram"]
    - path: "src/queue/models.py"
      provides: "JobStatus.NEEDS_HUMAN member and AuditEvent.NEEDS_HUMAN entry"
      contains: "NEEDS_HUMAN"
    - path: "src/browser/linkedin_applier.py"
      provides: "Updated selector constants, frame-aware _navigate_modal, reCAPTCHA detection, RecaptchaDetected exception"
      contains: "RecaptchaDetected"
    - path: "src/api/routes/apply/linkedin_apply.py"
      provides: "RecaptchaDetected handler: NEEDS_HUMAN transition + Telegram call + paused_human response"
      contains: "paused_human"
    - path: "tests/unit/test_linkedin_sdui_fixes.py"
      provides: "Unit tests for selector predicate, reCAPTCHA detection, NEEDS_HUMAN state"
  key_links:
    - from: "src/browser/linkedin_applier.py"
      to: "src/notify.py"
      via: "import send_telegram — NOT used directly; notify is called by the route layer, not the applier"
      pattern: "RecaptchaDetected"
    - from: "src/api/routes/apply/linkedin_apply.py"
      to: "src/notify.py"
      via: "await send_telegram(...)"
      pattern: "await send_telegram"
    - from: "src/api/routes/apply/linkedin_apply.py"
      to: "src/queue/models.py"
      via: "JobStatus.NEEDS_HUMAN"
      pattern: "NEEDS_HUMAN"
    - from: "src/api/routes/apply/queued_linkedin_jobs endpoint"
      to: "src/queue/models.py"
      via: "WHERE status NOT IN (NEEDS_HUMAN)"
      pattern: "NEEDS_HUMAN"
---

<objective>
Fix the LinkedIn Easy Apply flow against the live SDUI reality captured in 03-SDUI-FINDINGS.md.
Three concrete breakages must be resolved: (1) the trigger selector is dead, (2) modal navigation
operates on the wrong frame, (3) reCAPTCHA Enterprise is present and requires a human-pause path.

Purpose: The shipped 03-01..03-04 applier cannot open the Easy Apply modal at all. This plan
restores end-to-end functionality against the actual live LinkedIn SDUI and adds a safe reCAPTCHA
pause loop so autonomous applies are not silently bot-scored into rejection.

Output:
  - src/notify.py — thin Telegram send helper (no new package dep; uses httpx already in pyproject)
  - src/queue/models.py — NEEDS_HUMAN added to JobStatus
  - src/audit_log.py — NEEDS_HUMAN added to AuditEvent
  - src/browser/linkedin_applier.py — selector fix, frame-aware modal, RecaptchaDetected exception
  - src/api/routes/apply/linkedin_apply.py — RecaptchaDetected handler + paused_human response
  - tests/unit/test_linkedin_sdui_fixes.py — unit tests for the above
  - Human checkpoint: live browser re-validation using scripts/validate_easy_apply_selector.py
    and scripts/capture_sdui_apply.py against a real QUEUED job, stopping before actual submit.
</objective>

<execution_context>
@/Users/stefano/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stefano/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/stefano/Documents/Workspaces/Job Application Automation/.planning/PROJECT.md
@/Users/stefano/Documents/Workspaces/Job Application Automation/.planning/ROADMAP.md
@/Users/stefano/Documents/Workspaces/Job Application Automation/.planning/phases/03-linkedin-easy-apply/03-SDUI-FINDINGS.md
@/Users/stefano/Documents/Workspaces/Job Application Automation/.planning/phases/03-linkedin-easy-apply/03-CONTEXT.md

<interfaces>
<!-- Key contracts extracted from the shipped modules. Executors use these directly. -->

From src/queue/models.py — JobStatus enum (current, before this plan):
  DISCOVERED, QUEUED, REJECTED, APPLYING, SUBMITTED, FAILED, SKIPPED

From src/audit_log.py — AuditEvent enum (current):
  DISCOVERED, FILTERED_PASS, FILTERED_REJECT, DEDUP_SKIP, QUEUED,
  DRY_RUN_WOULD_QUEUE, DRY_RUN_WOULD_REJECT, APPLYING, SUBMITTED,
  FAILED, SKIPPED, NOTIFIED

From src/browser/linkedin_applier.py — public exceptions:
  ChallengeDetected(Exception)   — checkpoint / authwall / login redirect
  NoEasyApplyButton(Exception)   — Easy Apply trigger not found (D-01 expected skip)
  UnknownFormField(Exception)    — unmappable form field label (D-11 expected skip)
  ModalNavigationError(Exception) — no Next/Review/Submit found, or loop cap exceeded

From src/browser/linkedin_applier.py — constants to REPLACE:
  EASY_APPLY_BTN_XPATH = '//button[contains(@class, "jobs-apply-button")]'  # DEAD — replace

From src/api/routes/apply/linkedin_apply.py — endpoint signature:
  POST /linkedin-easy-apply  →  {"status": "submitted"|"skipped", "job_id": ...}
  Current exception handling: ChallengeDetected → HTTP 503, NoEasyApplyButton/UnknownFormField → 200 skipped
  New branch needed: RecaptchaDetected → HTTP 200, {"status": "paused_human", "job_id": ...}

From pyproject.toml — already present: httpx>=0.25
  Use httpx.AsyncClient for Telegram Bot API calls in src/notify.py — no new package needed.

Environment variables (Telegram, per 03-CONTEXT.md patterns):
  TELEGRAM_BOT_TOKEN   — bot token from BotFather
  TELEGRAM_CHAT_ID     — Stefano's personal chat ID
  Telegram Bot API endpoint: https://api.telegram.org/bot{TOKEN}/sendMessage
  Body: {"chat_id": CHAT_ID, "text": message, "parse_mode": "HTML"}

Camoufox frame access pattern (Playwright-compatible):
  page.frames  — list of all frames in the page
  frame = next((f for f in page.frames if "apply" in f.url.lower() or f.name == "..."), None)
  OR: modal_locator = page.locator("div[data-test-modal]")
      frame = await modal_locator.frame_locator("iframe").first  # if modal uses iframe
  Live finding (03-SDUI-FINDINGS.md §2): modal is a dynamic overlay inside a frame/overlay —
  the applier must locate the frame that contains modal content and drive locators against it.
  Recommended probe: after modal appears, iterate page.frames checking for a frame whose
  url contains "linkedin.com" and whose content has the "Contact info" heading or
  aria-label="Continue to next step". If no dedicated frame is found (modal is in main frame
  as an overlay div), fall back to page itself — but the probe must run first.

reCAPTCHA detection signal (03-SDUI-FINDINGS.md §4):
  frame[4] in the apply flow loads google.com/recaptcha/enterprise/...
  Detection: any(("recaptcha" in f.url.lower() and "enterprise" in f.url.lower())
              for f in page.frames)
  This check runs AFTER the modal appears, BEFORE the first fill_form_fields call.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add NEEDS_HUMAN state, Telegram notify helper, and RecaptchaDetected exception</name>
  <files>
    src/queue/models.py
    src/audit_log.py
    src/notify.py
    src/browser/linkedin_applier.py
    tests/unit/test_linkedin_sdui_fixes.py
  </files>
  <behavior>
    - JobStatus.NEEDS_HUMAN is a valid member of the JobStatus StrEnum.
    - AuditEvent.NEEDS_HUMAN is a valid member of the AuditEvent StrEnum.
    - send_telegram(text) is an async function in src/notify.py. When
      TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID are set, it posts to the Bot API.
      When either env var is absent, it logs a warning and returns without raising.
    - RecaptchaDetected is a new exception class in src/browser/linkedin_applier.py
      that can be raised and caught independently of ChallengeDetected.
    - detect_recaptcha(page) is a synchronous predicate in linkedin_applier that returns
      True when any frame in page.frames has a URL containing both "recaptcha" and "enterprise".
    Unit test cases (tests/unit/test_linkedin_sdui_fixes.py):
      - test_needs_human_in_job_status: JobStatus("NEEDS_HUMAN") == JobStatus.NEEDS_HUMAN
      - test_needs_human_in_audit_event: AuditEvent("NEEDS_HUMAN") == AuditEvent.NEEDS_HUMAN
      - test_recaptcha_detected_is_exception: issubclass(RecaptchaDetected, Exception)
      - test_detect_recaptcha_true: page mock with one frame url="https://www.google.com/recaptcha/enterprise/v3"
        → detect_recaptcha(page) returns True
      - test_detect_recaptcha_false: page mock with frame url="https://www.linkedin.com/jobs/view/"
        → detect_recaptcha(page) returns False
      - test_send_telegram_missing_env: with TELEGRAM_BOT_TOKEN unset, send_telegram("hi") returns
        without raising (monkeypatch os.environ)
  </behavior>
  <action>
    Step 1 — src/queue/models.py: Add NEEDS_HUMAN = "NEEDS_HUMAN" to the JobStatus enum,
    immediately after SKIPPED. No other changes to the file.

    Step 2 — src/audit_log.py: Add NEEDS_HUMAN = "NEEDS_HUMAN" to the AuditEvent StrEnum,
    immediately after SKIPPED. No other changes to the file.

    Step 3 — src/notify.py (new file): Create an async send_telegram(text: str) -> None function.
    Import httpx and os. Read TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID from os.environ. If either
    is absent or empty, emit structlog.get_logger().warning("telegram_env_missing") and return.
    Otherwise POST to https://api.telegram.org/bot{TOKEN}/sendMessage with JSON body
    {"chat_id": CHAT_ID, "text": text, "parse_mode": "HTML"} using an async httpx.AsyncClient
    with a 10-second timeout. Log telegram_sent on 2xx or telegram_send_failed with status_code
    on non-2xx. Do NOT raise on failure — notification is best-effort. Add module-level docstring
    referencing T-03-SC (no secrets in logs).

    Step 4 — src/browser/linkedin_applier.py additions (do not remove anything existing yet):
    - Add RecaptchaDetected(Exception) class after ModalNavigationError, with docstring
      "Raised when reCAPTCHA Enterprise is detected in the apply flow — caller must pause the job."
    - Add detect_recaptcha(page: Any) -> bool function. Implementation: return any(
        "recaptcha" in f.url.lower() and "enterprise" in f.url.lower()
        for f in getattr(page, "frames", [])
      ). This is a synchronous function (frames is a property, not a coroutine in Playwright).

    Step 5 — Write tests FIRST (TDD), then verify they go RED before implementing. Tests live
    in tests/unit/test_linkedin_sdui_fixes.py. Cover all six behaviors listed in the behavior
    block above. Use unittest.mock.MagicMock to build the page stub for detect_recaptcha tests.
    For send_telegram test, monkeypatch os.environ to remove TELEGRAM_BOT_TOKEN and call
    asyncio.run or pytest-asyncio async test. All tests must be importable without a live DB
    or browser.
  </action>
  <verify>
    <automated>cd "/Users/stefano/Documents/Workspaces/Job Application Automation" && python -m pytest tests/unit/test_linkedin_sdui_fixes.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All six unit tests in test_linkedin_sdui_fixes.py pass. JobStatus.NEEDS_HUMAN and
    AuditEvent.NEEDS_HUMAN are importable. RecaptchaDetected and detect_recaptcha are
    importable from src.browser.linkedin_applier. src/notify.py exports send_telegram.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix trigger selector, frame-aware modal navigation, and reCAPTCHA pause in applier + route</name>
  <files>
    src/browser/linkedin_applier.py
    src/api/routes/apply/linkedin_apply.py
    scripts/validate_easy_apply_selector.py
    tests/unit/test_linkedin_sdui_fixes.py
  </files>
  <behavior>
    Selector behavior:
      - EASY_APPLY_BTN_XPATH is replaced by two new constants:
          EASY_APPLY_TEXT_SELECTOR = "text=Easy Apply"
          EASY_APPLY_ARIA_SELECTOR = "[aria-label*='Easy Apply' i]"
        Neither uses a class name or href.
      - _find_and_click_easy_apply tries EASY_APPLY_TEXT_SELECTOR first (count > 0 + is_visible),
        then EASY_APPLY_ARIA_SELECTOR. If neither matches any visible element, raises NoEasyApplyButton.
      - After clicking the trigger, the method calls page.wait_for_selector with a confirmed
        modal CSS selector (div[data-test-modal], or role=dialog with LinkedIn-modal attribute)
        with a 8-second timeout. On TimeoutError it retries the click once more, then
        wait_for_selector again. On second failure raises ModalNavigationError("modal_open_timeout").
    Frame-awareness behavior:
      - After the modal is confirmed open, _resolve_modal_frame(page) inspects page.frames:
          For each frame: if "linkedin.com" in frame.url and await frame.title() contains
          "Contact" OR frame.locator("[aria-label='Continue to next step']").count() > 0,
          return that frame. If no dedicated frame qualifies, return page itself (overlay pattern).
      - _navigate_modal receives the resolved frame (not always page) and issues all locator
        calls — fill_form_fields, submit_btn, review_btn, next_btn — against frame, not page.
      - fill_form_fields signature adds a frame parameter: fill_form_fields(frame, profile, ...).
        All internal locator calls use frame.locator(...) instead of page.locator(...).
    reCAPTCHA pause behavior:
      - detect_recaptcha(page) MUST run at the TOP OF EACH loop iteration in _navigate_modal,
        before that iteration's fill_form_fields call — NOT just once before the first fill.
        Live finding: reCAPTCHA Enterprise can appear mid-flow (after Contact info, on a later
        step), so a single pre-loop check would let a later challenge slip through and submit.
        Always check against the top-level page (recaptcha loads as a sibling frame), not the
        resolved modal frame. If True at any iteration, raise RecaptchaDetected("recaptcha_enterprise")
        before filling or clicking anything on that step.
      - The route (linkedin_apply.py) catches RecaptchaDetected: transitions job to NEEDS_HUMAN,
        writes AuditEvent.NEEDS_HUMAN, awaits send_telegram with the alert message defined in
        03-CONTEXT.md specifics section (substituting challenge type = "recaptcha_enterprise"),
        returns HTTP 200 {"status": "paused_human", "job_id": ...}. Does NOT raise HTTPException.
      - queued_linkedin_jobs endpoint filters out NEEDS_HUMAN jobs
        (WHERE status = QUEUED, not NEEDS_HUMAN — current query already does this correctly since
        NEEDS_HUMAN != QUEUED, so no query change needed — verify with a comment).
    Additional unit tests to add to test_linkedin_sdui_fixes.py:
      - test_find_easy_apply_uses_text_selector: mock page.locator("text=Easy Apply") returns
        count=1, is_visible=True → _find_and_click_easy_apply does not raise; click is called once.
      - test_find_easy_apply_falls_back_to_aria: mock text selector count=0, aria selector count=1
        → _find_and_click_easy_apply resolves via aria without raising.
      - test_find_easy_apply_raises_when_absent: both selectors return count=0 → NoEasyApplyButton raised.
      - test_navigate_modal_detects_recaptcha: page mock with recaptcha enterprise frame present
        at the FIRST iteration → RecaptchaDetected raised before any fill call.
      - test_navigate_modal_detects_recaptcha_mid_flow: page mock where the recaptcha frame is
        ABSENT on iteration 1 (fills Contact info, clicks Next) but PRESENT on iteration 2 →
        RecaptchaDetected raised on iteration 2 before the second fill, and submit is never clicked.
  </behavior>
  <action>
    src/browser/linkedin_applier.py:

    1. Replace EASY_APPLY_BTN_XPATH constant with two constants:
         EASY_APPLY_TEXT_SELECTOR = "text=Easy Apply"
         EASY_APPLY_ARIA_SELECTOR = "[aria-label*='Easy Apply' i]"
       Remove the old EASY_APPLY_BTN_XPATH line entirely.

    2. Rewrite _find_and_click_easy_apply:
         For each selector in [EASY_APPLY_TEXT_SELECTOR, EASY_APPLY_ARIA_SELECTOR]:
           locator = page.locator(selector)
           if await locator.count() > 0:
             first = locator.first
             if await first.is_visible():
               await first.click()
               log.info("li_easy_apply_clicked", selector=selector)
               # Wait for modal; retry once on timeout
               MODAL_SELECTOR = "div[data-test-modal], div[role='dialog']"
               for attempt in range(2):
                 try:
                   await page.wait_for_selector(MODAL_SELECTOR, timeout=8000)
                   return  # modal confirmed open
                 except Exception:
                   if attempt == 0:
                     log.warning("li_modal_open_retry", selector=selector)
                     await first.click()  # retry the trigger click
                   else:
                     raise ModalNavigationError("modal_open_timeout")
         raise NoEasyApplyButton("Easy Apply trigger not found — neither text nor aria matched")

    3. Add _resolve_modal_frame(self, page: Any) -> Any as an async method on LinkedInApplier:
         for frame in page.frames:
           if "linkedin.com" not in frame.url.lower():
             continue
           try:
             count = await frame.locator("[aria-label='Continue to next step']").count()
             if count > 0:
               return frame
             title = await frame.title()
             if "contact" in title.lower():
               return frame
           except Exception:
             continue
         return page  # EXPECTED outcome for the current live structure: the modal is an
         # overlay div in the MAIN document, not a separate iframe. Per 03-SDUI-FINDINGS.md §2,
         # `return page` is the normal path, not an exceptional fallback. The frame probe above
         # is defensive in case LinkedIn moves the modal into a dedicated iframe later. Do not
         # treat "no frame resolved" as an error during Task 3 validation.

    4. Update fill_form_fields signature to accept frame as its first positional parameter
       instead of page. Replace ALL internal page.locator(...) calls with frame.locator(...).
       Update the function docstring to note the frame parameter.

    5. Rewrite _navigate_modal:
         - Before the loop: frame = await self._resolve_modal_frame(page)
         - INSIDE the loop, as the FIRST statement of every iteration:
             if detect_recaptcha(page): raise RecaptchaDetected("recaptcha_enterprise")
           (top-level page, not frame — catches challenges that appear on any step, not just open)
         - Replace all fill_form_fields(page, ...) calls with fill_form_fields(frame, ...)
         - Replace all page.locator("button[aria-label=...") submit/review/next lookups with
           frame.locator("button[aria-label=...]")
         - Keep the MAX_PAGES = 20 guard and the wait_for_load_state fallback unchanged.

    6. Update the apply() method call site for fill_form_fields if it is called directly there
       (it is not — it's called inside _navigate_modal, so no change at apply() level needed).

    src/api/routes/apply/linkedin_apply.py:

    scripts/validate_easy_apply_selector.py:

    6b. This script imports the now-deleted EASY_APPLY_BTN_XPATH at module level (line ~16),
        so it will ImportError the moment Task 2 removes that constant — and pytest collection
        scanning scripts/ would also break. Update the import to EASY_APPLY_TEXT_SELECTOR and
        change the script's selector-under-test reference accordingly. This MUST happen in Task 2
        (same commit that removes the constant), not deferred to the Task 3 checkpoint.

    src/api/routes/apply/linkedin_apply.py:

    7. Add RecaptchaDetected to the import from src.browser.linkedin_applier.

    8. Add except RecaptchaDetected block after the ChallengeDetected block:
         - Open a new session.begin() block
         - Fetch the job row by payload.job_id
         - Set job.status = JobStatus.NEEDS_HUMAN, job.rejection_reason = "recaptcha_enterprise",
           job.updated_at = datetime.utcnow()
         - Await write_audit(session, source="api", event=AuditEvent.NEEDS_HUMAN,
                             job_id=payload.job_id, reason="recaptcha_enterprise")
         - After the DB commit block, await send_telegram(
             f"LinkedIn reCAPTCHA detected for job {payload.job_id} — job paused. "
             f"Resolve manually, then requeue the job."
           )
         - Return JSONResponse(status_code=200,
             content={"status": "paused_human", "job_id": payload.job_id})
       Import send_telegram from src.notify at the top of the file.
       Import AuditEvent.NEEDS_HUMAN and JobStatus.NEEDS_HUMAN — these are already on the
       imported enums once models.py and audit_log.py are updated in Task 1.

    tests/unit/test_linkedin_sdui_fixes.py — add four new test functions as described in
    the behavior block. Use AsyncMock for page.locator(...).count and .is_visible and
    .click. The test for recaptcha detection in modal context (test_navigate_modal_detects_recaptcha)
    should call applier._navigate_modal with a page mock whose frames list contains a mock frame
    with url="https://www.google.com/recaptcha/enterprise/anchor" — assert RecaptchaDetected raised.
  </action>
  <verify>
    <automated>cd "/Users/stefano/Documents/Workspaces/Job Application Automation" && python -m pytest tests/unit/test_linkedin_sdui_fixes.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    All eleven unit tests in test_linkedin_sdui_fixes.py pass (six from Task 1 + five new ones,
    including the mid-flow reCAPTCHA test). EASY_APPLY_BTN_XPATH is absent from linkedin_applier.py
    (grep returns no match) AND from scripts/validate_easy_apply_selector.py (import updated).
    fill_form_fields first positional parameter is named "frame" not "page".
    detect_recaptcha is called at the top of _navigate_modal before any fill call.
    RecaptchaDetected is imported and handled in linkedin_apply.py.
    python -m mypy src/browser/linkedin_applier.py src/notify.py src/api/routes/apply/linkedin_apply.py
    exits 0 or with only expected ignored-import errors (camoufox stubs).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live browser re-validation — confirm selectors work against real LinkedIn session</name>
  <what-built>
    Tasks 1 and 2 reworked the Easy Apply trigger selector, made modal navigation frame-aware,
    and added reCAPTCHA detection + pause. The new code has never been exercised against the
    live LinkedIn session. This checkpoint confirms the fixes work before any further automation.
  </what-built>
  <how-to-verify>
    Run the two validation scripts against the same job URL used during the SDUI capture
    (https://www.linkedin.com/jobs/view/4418669306/ or any currently-open Easy Apply job):

    Step 1 — Selector validation (confirms the trigger is found):
      cd "/Users/stefano/Documents/Workspaces/Job Application Automation"
      PYTHONPATH=. LINKEDIN_PROFILE_DIR=data/linkedin_profile uv run python \
        scripts/validate_easy_apply_selector.py "https://www.linkedin.com/jobs/view/4418669306/"

    NOTE: both scripts REQUIRE the job URL as argv[1], PYTHONPATH=. (to import src.*),
    LINKEDIN_PROFILE_DIR (the saved session), and `uv run` (for the pinned deps). Bare
    `python scripts/...` fails with ModuleNotFoundError / sys.exit(1) — confirmed live.
    NOTE: Task 2 already updated validate_easy_apply_selector.py's import from the deleted
    EASY_APPLY_BTN_XPATH to EASY_APPLY_TEXT_SELECTOR, so it runs against the new selector.

    Expected output: a non-empty list of matching elements (count >= 1) and no "NOT FOUND" lines.

    Step 2 — Modal capture (confirms modal opens and frame is resolved):
      PYTHONPATH=. LINKEDIN_PROFILE_DIR=data/linkedin_profile uv run python \
        scripts/capture_sdui_apply.py "https://www.linkedin.com/jobs/view/4418669306/"

    Expected output: modal title visible ("Apply to ..."), step title visible ("Contact info"),
    progress bar at 0%, "Next" button located. The script must NOT submit — verify it exits
    after capturing without clicking Next.

    Step 3 — reCAPTCHA probe (confirms detection fires correctly):
    While capture_sdui_apply.py has the modal open (or by re-running it with a print patch),
    verify the script output shows "recaptcha_enterprise_frame_detected: True" OR manually
    inspect the page.frames list and confirm at least one frame URL contains "recaptcha"
    and "enterprise".

    The run must NOT reach the Submit button. Stop the script or confirm it exits before submit.
    If any step fails, describe the error output and the exact line it fails at.
  </how-to-verify>
  <resume-signal>
    Type "validated" if all three steps pass. Describe any failure with the script output.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| applier → LinkedIn SDUI | Browser automation; LinkedIn can change DOM structure per deploy |
| route → Telegram Bot API | Outbound HTTP; token in env var, never in logs or payloads |
| n8n → /linkedin-easy-apply | Inbound API call; guarded by verify_api_key (T-03-08, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Spoofing | EASY_APPLY_TEXT_SELECTOR "text=Easy Apply" | accept | Localized UI uses Tagalog aria-label but inner span text is confirmed English; NoEasyApplyButton raised on miss, job is SKIPPED |
| T-03-05-02 | Tampering | fill_form_fields frame parameter | mitigate | _resolve_modal_frame falls back to page if no qualifying frame found — prevents None dereference |
| T-03-05-03 | Elevation | RecaptchaDetected handler sets NEEDS_HUMAN | mitigate | DB transition is audited via write_audit(NEEDS_HUMAN); job cannot be re-queued automatically |
| T-03-05-04 | Info Disclosure | send_telegram logging | mitigate | log only job_id and event name; message text is never logged to structlog; TELEGRAM_BOT_TOKEN never appears in logs |
| T-03-05-05 | Denial of Service | Telegram API failure blocks route | mitigate | send_telegram catches all exceptions and returns; HTTP 200 paused_human is returned regardless of Telegram success |
| T-03-05-SC | Tampering | No new npm/pip/cargo installs in this plan | accept | httpx already pinned in pyproject.toml; no package legitimacy audit needed |
</threat_model>

<verification>
Run the full test suite to confirm no regressions from the existing 03-01..03-04 plans:

  cd "/Users/stefano/Documents/Workspaces/Job Application Automation"
  python -m pytest tests/unit/ tests/integration/ -v --tb=short 2>&1 | tail -40

Confirm EASY_APPLY_BTN_XPATH no longer exists:

  grep -rn "EASY_APPLY_BTN_XPATH\|jobs-apply-button" \
    "/Users/stefano/Documents/Workspaces/Job Application Automation/src/" \
    | grep -v "\.pyc"

Expected: zero matches.

Confirm NEEDS_HUMAN appears in both models and audit_log:

  grep -n "NEEDS_HUMAN" \
    "/Users/stefano/Documents/Workspaces/Job Application Automation/src/queue/models.py" \
    "/Users/stefano/Documents/Workspaces/Job Application Automation/src/audit_log.py"

Expected: one match in each file.

Confirm RecaptchaDetected is handled in the route:

  grep -n "RecaptchaDetected\|paused_human" \
    "/Users/stefano/Documents/Workspaces/Job Application Automation/src/api/routes/apply/linkedin_apply.py"

Expected: at least two matches (except clause + response content).
</verification>

<success_criteria>
- python -m pytest tests/unit/test_linkedin_sdui_fixes.py passes with 11 tests (0 failures, 0 errors).
- reCAPTCHA is re-checked at the top of every _navigate_modal iteration (mid-flow challenges caught).
- python -m pytest tests/unit/ tests/integration/ exits 0 (no regressions).
- EASY_APPLY_BTN_XPATH is absent from src/browser/linkedin_applier.py.
- fill_form_fields operates on a frame/overlay locator, not unconditionally on page.
- detect_recaptcha(page) returns True when any page.frames entry URL contains "recaptcha" and "enterprise".
- POST /apply/linkedin-easy-apply returns {"status": "paused_human", "job_id": ...} (HTTP 200)
  when RecaptchaDetected is raised by the applier.
- Job with reCAPTCHA detection is written to DB with status=NEEDS_HUMAN and audit event NEEDS_HUMAN.
- Live validation checkpoint passes: scripts/validate_easy_apply_selector.py finds the trigger,
  scripts/capture_sdui_apply.py opens the modal and locates the frame with Contact info fields,
  without submitting.
</success_criteria>

<output>
Create `.planning/phases/03-linkedin-easy-apply/03-05-SUMMARY.md` when done.
</output>
