---
phase: 02-ingest-generate-and-email-apply
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - n8n/workflows/gmail-ingest.json
  - n8n/workflows/jobspy-scrape.json
  - n8n/workflows/kalibrr-scrape.json
  - n8n/workflows/ai-apply-pipeline.json
  - n8n/workflows/error-handler.json
  - n8n/workflows/heartbeat.json
  - n8n/README.md
  - src/api/routes/application.py
  - src/api/routes/resume.py
  - .gitignore
autonomous: false
requirements: [INGEST-01, INGEST-02, INGEST-03, AI-01, AI-02, AI-03, APPLY-02, NOTIF-01, NOTIF-02, OPS-01, OPS-02]

user_setup:
  - service: n8n
    why: "n8n orchestrates all workflows — must be running to trigger pipelines"
    env_vars:
      - name: N8N_ENCRYPTION_KEY
        source: "Generate locally: openssl rand -hex 32. Store in .env. NEVER regenerate after first run."
    dashboard_config:
      - task: "Import all workflow JSON files from n8n/workflows/ via the n8n UI"
        location: "n8n UI (localhost:5678) -> Workflows -> Import from File"
      - task: "Configure Anthropic API credential"
        location: "n8n UI -> Credentials -> Add Credential -> Header Auth -> Name: anthropicApi, Header: x-api-key, Value: your Anthropic API key"
      - task: "Configure Telegram Bot credential"
        location: "n8n UI -> Credentials -> Add Credential -> Telegram -> Bot Token from @BotFather"
      - task: "Configure Gmail OAuth2 credential (for Send Email)"
        location: "n8n UI -> Credentials -> Add Credential -> Gmail OAuth2"
  - service: telegram
    why: "Telegram bot for submission notifications and failure alerts"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Telegram -> @BotFather -> /newbot -> copy token"
      - name: TELEGRAM_CHAT_ID
        source: "Send /start to your bot, then visit https://api.telegram.org/bot<TOKEN>/getUpdates to find chat_id"
    dashboard_config:
      - task: "Create Telegram bot via BotFather"
        location: "Telegram -> search @BotFather -> /newbot"
      - task: "Send /start to the bot to initialize the chat"
        location: "Telegram -> your new bot -> /start"
  - service: anthropic
    why: "Claude API for email parsing, resume selection, cover letter generation"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com -> API Keys -> Create Key"

must_haves:
  truths:
    - "n8n gmail-ingest workflow polls /poll-gmail hourly, parses each email with Claude Haiku, and feeds extracted jobs to /ingest-lead with apply_type='email'"
    - "n8n jobspy-scrape workflow triggers /scrape-jobspy every 4 hours and feeds results to /ingest-lead"
    - "n8n kalibrr-scrape workflow triggers /scrape-kalibrr every 4 hours and feeds results to /ingest-lead"
    - "n8n ai-apply-pipeline workflow takes QUEUED jobs, calls GET /profile for real profile data, calls /select-resume, calls /generate-screening-answers when screening_questions is non-empty, generates cover letter via Claude Sonnet using profile from /profile, sends application email with resume attachment, calls /write-application and /mark-submitted, and fires Telegram notification"
    - "n8n error-handler workflow catches any workflow error and sends a Telegram alert (NOTIF-02)"
    - "n8n heartbeat workflow sends a Telegram ping every 30 minutes (OPS-02)"
    - "n8n detects HTTP 503 challenge_detected responses and fires Telegram alert instead of continuing (OPS-01)"
  artifacts:
    - path: "n8n/workflows/gmail-ingest.json"
      provides: "Gmail polling + Claude email parsing + /ingest-lead loop with apply_type='email'"
    - path: "n8n/workflows/jobspy-scrape.json"
      provides: "JobSpy scraping + /ingest-lead loop"
    - path: "n8n/workflows/kalibrr-scrape.json"
      provides: "Kalibrr scraping + /ingest-lead loop"
    - path: "n8n/workflows/ai-apply-pipeline.json"
      provides: "Profile fetch + resume selection + screening answers + cover letter + email send + state transitions + Telegram"
    - path: "n8n/workflows/error-handler.json"
      provides: "Error trigger -> Telegram alert"
    - path: "n8n/workflows/heartbeat.json"
      provides: "30-min schedule -> Telegram heartbeat ping"
    - path: "n8n/README.md"
      provides: "Import instructions and credential setup guide"
  key_links:
    - from: "n8n/workflows/gmail-ingest.json"
      to: "src/api/routes/gmail.py"
      via: "HTTP Request POST http://api:8000/poll-gmail"
      pattern: "poll-gmail"
    - from: "n8n/workflows/gmail-ingest.json"
      to: "src/api/routes/ingest.py"
      via: "HTTP Request POST http://api:8000/ingest-lead with apply_type='email'"
      pattern: "apply_type"
    - from: "n8n/workflows/ai-apply-pipeline.json"
      to: "src/api/routes/profile.py"
      via: "HTTP Request GET http://api:8000/profile"
      pattern: "profile"
    - from: "n8n/workflows/ai-apply-pipeline.json"
      to: "src/api/routes/resume.py"
      via: "HTTP Request POST http://api:8000/select-resume"
      pattern: "select-resume"
    - from: "n8n/workflows/ai-apply-pipeline.json"
      to: "src/api/routes/application.py"
      via: "HTTP Request POST http://api:8000/generate-screening-answers"
      pattern: "generate-screening-answers"
    - from: "n8n/workflows/ai-apply-pipeline.json"
      to: "Anthropic API"
      via: "HTTP Request POST https://api.anthropic.com/v1/messages"
      pattern: "claude-sonnet"
---

## Phase Goal

**As a** job seeker, **I want to** have job leads automatically scraped, filtered, matched to my best resume, and applied to via email with a tailored cover letter and Telegram notification, **so that** I never miss an eligible opportunity.

<objective>
Create all six n8n workflow JSON files that wire the FastAPI endpoints into automated pipelines: Gmail ingestion (with apply_type='email' on ingested leads), JobSpy scraping, Kalibrr scraping, the AI-powered apply pipeline (profile fetch + resume selection + screening answers + cover letter + email send + Telegram notification), error handling, and heartbeat monitoring. Add two small supporting GET endpoints to FastAPI. This plan completes the full autonomous loop.

Purpose: All FastAPI endpoints from Plans 01-04 are operational but passive — they only respond to HTTP calls. n8n provides the scheduling, orchestration, and notification layer that makes the system autonomous. Without these workflows, nothing runs on its own.
Output: Six importable n8n workflow JSON files, two supporting GET endpoints, a README with setup instructions, and a human-verified end-to-end test.
</objective>

<execution_context>
@/Users/stefano/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stefano/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ingest-generate-and-email-apply/02-CONTEXT.md
@.planning/phases/02-ingest-generate-and-email-apply/02-RESEARCH.md
@.planning/phases/02-ingest-generate-and-email-apply/02-PATTERNS.md
@.planning/phases/02-ingest-generate-and-email-apply/02-01-SUMMARY.md
@.planning/phases/02-ingest-generate-and-email-apply/02-02-SUMMARY.md
@.planning/phases/02-ingest-generate-and-email-apply/02-03-SUMMARY.md
@.planning/phases/02-ingest-generate-and-email-apply/02-04-SUMMARY.md

<interfaces>
<!-- FastAPI endpoints from Plans 01-04 that n8n calls -->

POST http://api:8000/poll-gmail -> PollGmailOut {message_ids: list[str], history_id: str}
POST http://api:8000/fetch-email-body {message_id: str} -> FetchEmailBodyOut {body_text: str, subject: str|null, sender: str|null}
POST http://api:8000/ingest-lead {url, title, company, location, source, clean_jd, apply_type} -> LeadOut {status: str, job_id: str|null}
POST http://api:8000/scrape-jobspy {search_term, location, results_wanted, hours_old, site_names} -> {jobs: list, count: int, warning?: str}
POST http://api:8000/scrape-kalibrr {search_term, max_pages} -> {jobs: list, count: int, warning?: str}
GET  http://api:8000/profile -> ProfileOut {summary, target_roles, key_projects, skills, location_preference, availability}
POST http://api:8000/select-resume {job_id, job_description, job_title, company} -> SelectResumeOut {resume_name: str, resume_text: str}
POST http://api:8000/generate-screening-answers {job_id, screening_questions, job_description, job_title} -> GenerateScreeningAnswersOut {job_id: str, answers: list[dict]}
POST http://api:8000/write-application {job_id, resume_name, cover_letter} -> {status: str, job_id: str}
POST http://api:8000/mark-submitted {job_id} -> {status: str, job_id: str}

Anthropic API (called directly by n8n):
POST https://api.anthropic.com/v1/messages
Headers: x-api-key, anthropic-version: 2023-06-01, content-type: application/json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ingestion workflows (gmail-ingest, jobspy-scrape, kalibrr-scrape) + error-handler + heartbeat + supporting GET endpoints</name>
  <files>
    n8n/workflows/gmail-ingest.json,
    n8n/workflows/jobspy-scrape.json,
    n8n/workflows/kalibrr-scrape.json,
    n8n/workflows/error-handler.json,
    n8n/workflows/heartbeat.json,
    src/api/routes/application.py,
    src/api/routes/resume.py,
    .gitignore
  </files>
  <read_first>
    .planning/phases/02-ingest-generate-and-email-apply/02-RESEARCH.md
    .planning/phases/02-ingest-generate-and-email-apply/02-CONTEXT.md
    docker-compose.yml
    src/api/routes/application.py
    src/api/routes/resume.py
    src/api/schemas.py
    src/queue/models.py
    .gitignore
  </read_first>
  <action>
    Create n8n/workflows/ directory.

    First, add two supporting GET endpoints needed by the n8n workflows:

    Add GET /queued-email-jobs to src/api/routes/application.py. This endpoint takes no request body. It queries the jobs table: SELECT * FROM jobs WHERE status="QUEUED" AND apply_type="email". Return a JSON list of dicts, each with keys: id, title, company, url, clean_jd, screening_questions (if present on the Job record), apply_type. Use the existing session dependency. If no jobs found, return empty list.

    Add GET /resume-file/{resume_name} to src/api/routes/resume.py. This endpoint takes resume_name as a path parameter. Construct the file path from request.app.state.resumes_dir / resume_name. Validate that the resolved path is within RESUMES_DIR (prevent path traversal: use Path.resolve() and check it starts with resumes_dir.resolve()). If file exists, return FileResponse with appropriate media_type (application/pdf for .pdf, application/vnd.openxmlformats-officedocument.wordprocessingml.document for .docx). If not found, return HTTP 404. Import FileResponse from fastapi.responses.

    Now create the n8n workflow JSON files:

    Create gmail-ingest.json as an n8n workflow JSON export per RESEARCH.md architecture diagram and Pattern 7. The workflow structure:
    1. Schedule Trigger node: cron every 1 hour per D-04
    2. HTTP Request node: POST http://api:8000/poll-gmail (no body). On HTTP 503, branch to Telegram alert per OPS-01.
    3. IF node: check if message_ids array length > 0. False branch ends.
    4. SplitInBatches node: iterate over each message_id in the array
    5. HTTP Request node: POST http://api:8000/fetch-email-body with body {"message_id": "{{ $json.message_id }}"}
    6. HTTP Request node: POST https://api.anthropic.com/v1/messages -- Claude Haiku extraction call per D-09, D-12. Headers: x-api-key from Header Auth credential named "anthropicApi", anthropic-version: 2023-06-01. Body per RESEARCH.md Pattern 5: model "claude-haiku-3-5", max_tokens 1024, system prompt instructing job listing extraction, user message content is the body_text from previous node. Include output_config.format with json_schema for jobs array with title/company/location/url fields.
    7. Parse JSON response. Extract the jobs array from Claude's response content[0].text.
    8. SplitInBatches node: iterate over each job in the parsed array
    9. HTTP Request node: POST http://api:8000/ingest-lead with body mapping title, company, location, url from the parsed job, source="linkedin_email", apply_type="email". The apply_type="email" field is set explicitly here because LinkedIn email leads are email-apply candidates by default (per D-22, only jobs with apply_type="email" reach the apply pipeline). On HTTP 503, branch to Telegram alert.

    Create jobspy-scrape.json per Pattern 7:
    1. Schedule Trigger: every 4 hours
    2. HTTP Request: POST http://api:8000/scrape-jobspy with body {"search_term": "Product Manager", "location": "Remote", "results_wanted": 50, "hours_old": 24, "site_names": ["indeed"]}. On 503, branch to Telegram alert.
    3. IF node: check for warning == "zero_results_possible_block". True branch: Telegram alert "JobSpy returned 0 results -- possible IP block" per Pitfall 3. False branch: continue.
    4. SplitInBatches: iterate jobs array
    5. HTTP Request: POST http://api:8000/ingest-lead with source="indeed", mapping title, company, location from job, url from job_url field, clean_jd from description field.

    Create kalibrr-scrape.json: identical pattern to jobspy-scrape.json but calls POST http://api:8000/scrape-kalibrr with {"search_term": "Product Manager", "max_pages": 3}. Source="kalibrr". URL field maps from url (not job_url).

    Create error-handler.json per RESEARCH.md Pattern 7 item 5:
    1. Error Trigger node (triggers when any workflow in the n8n instance errors)
    2. Telegram node: send message to configured chat_id with workflow name and error message. Use Telegram credential.

    Create heartbeat.json per OPS-02:
    1. Schedule Trigger: every 30 minutes
    2. Telegram node: send heartbeat confirmation with ISO timestamp to configured chat_id.

    All n8n workflow JSON files must use the n8n 2.x format (no Start node). All HTTP Request nodes targeting FastAPI use base URL http://api:8000 (Docker service name per RESEARCH.md anti-pattern warning -- NEVER localhost). All Anthropic API calls use Header Auth credential.

    Update .gitignore to add n8n_storage/ (n8n's internal data directory if created locally).

    Each workflow JSON should be a valid n8n export format with nodes array, connections object, and settings. Use reasonable node positioning (x/y coordinates). Set workflow name in the JSON to match the filename.
  </action>
  <verify>
    <automated>cd /Users/stefano/Documents/Workspaces/Job Application Automation && python -c "import json; [json.load(open(f'n8n/workflows/{f}')) for f in ['gmail-ingest.json','jobspy-scrape.json','kalibrr-scrape.json','error-handler.json','heartbeat.json']]; print('All 5 workflow JSONs are valid')" && uv run python -c "from src.api.routes.application import router; from src.api.routes.resume import router; print('Route imports OK')" && python -c "import json; wf=json.load(open('n8n/workflows/gmail-ingest.json')); nodes_str=json.dumps(wf); assert 'apply_type' in nodes_str, 'gmail-ingest must set apply_type'; print('gmail-ingest sets apply_type OK')"</automated>
  </verify>
  <acceptance_criteria>
    - GET /queued-email-jobs returns list of jobs with status=QUEUED and apply_type=email, including screening_questions field
    - GET /resume-file/{resume_name} returns file binary with correct content-type; 404 for missing file; path traversal blocked
    - n8n/workflows/gmail-ingest.json is valid JSON with Schedule Trigger (1h), /poll-gmail call, Claude Haiku extraction, /ingest-lead loop with apply_type="email" set in the request body
    - n8n/workflows/jobspy-scrape.json is valid JSON with Schedule Trigger (4h), /scrape-jobspy call, /ingest-lead loop, zero-result Telegram alert
    - n8n/workflows/kalibrr-scrape.json is valid JSON with Schedule Trigger (4h), /scrape-kalibrr call, /ingest-lead loop
    - n8n/workflows/error-handler.json is valid JSON with Error Trigger + Telegram node
    - n8n/workflows/heartbeat.json is valid JSON with Schedule Trigger (30min) + Telegram node
    - All HTTP Request nodes use http://api:8000 (not localhost)
    - Anthropic API calls include x-api-key header and anthropic-version: 2023-06-01
    - .gitignore includes n8n_storage/
  </acceptance_criteria>
  <done>Five n8n ingestion/ops workflow JSON files created with gmail-ingest setting apply_type='email', two supporting GET endpoints added to FastAPI, all valid JSON with correct Docker network URLs</done>
</task>

<task type="auto">
  <name>Task 2: AI apply pipeline workflow (with profile fetch + screening answers) + README</name>
  <files>
    n8n/workflows/ai-apply-pipeline.json,
    n8n/README.md
  </files>
  <read_first>
    n8n/workflows/gmail-ingest.json
    n8n/workflows/jobspy-scrape.json
    docker-compose.yml
    config/profile.yaml
    src/api/routes/application.py
    src/api/routes/resume.py
    src/api/routes/profile.py
    .planning/phases/02-ingest-generate-and-email-apply/02-CONTEXT.md
    .planning/phases/02-ingest-generate-and-email-apply/02-RESEARCH.md
  </read_first>
  <action>
    Create n8n/workflows/ai-apply-pipeline.json -- the core application workflow per RESEARCH.md architecture diagram "AI + Application Pipeline" section. Per D-19, D-20, D-21, D-22, D-24.

    Workflow structure:
    1. Schedule Trigger node: every 15 minutes (polls for QUEUED email-apply jobs)
    2. HTTP Request node: GET http://api:8000/profile. Returns ProfileOut JSON with summary, target_roles, key_projects, skills, location_preference, availability. This is called ONCE at the start of each pipeline run (not per-job) per D-19 to use config/profile.yaml as the single source of truth instead of a static block. Store the profile response in a Set node variable for use in downstream nodes.
    3. HTTP Request node: GET http://api:8000/queued-email-jobs. Returns list of jobs with apply_type="email" and status="QUEUED". If empty list, workflow ends via IF node check.
    4. IF node: check if returned array length > 0. False branch ends.
    5. SplitInBatches: iterate over each queued job
    6. HTTP Request node: POST http://api:8000/select-resume with body mapping job_id, job_description from clean_jd, job_title from title, company from the job object. Returns resume_name and resume_text.
    7. IF node: check if the current job has non-empty screening_questions. True branch goes to screening answers step. False branch skips to cover letter step.
    8. (Conditional) HTTP Request node: POST http://api:8000/generate-screening-answers with body mapping job_id, screening_questions from the job object, job_description from clean_jd, job_title from title. Returns GenerateScreeningAnswersOut with answers array. Per D-20, this calls Claude Haiku to generate answers using profile + JD context. This step only fires when the job has screening_questions.
    9. HTTP Request node: POST https://api.anthropic.com/v1/messages -- Claude Sonnet cover letter generation per D-13, D-19. Model: "claude-sonnet-4-5". System prompt: professional cover letter writer instructions. The user message includes: job_title, company, job_description, resume_text from /select-resume, and the FULL profile data from the GET /profile response (summary, target_roles, key_projects, skills, location_preference, availability). Using the profile from /profile ensures config/profile.yaml is the single source of truth -- no static block to maintain. Max_tokens: 2048. Headers: x-api-key from anthropicApi credential, anthropic-version: 2023-06-01.
    10. Extract cover_letter text from Claude response content[0].text.
    11. HTTP Request node: POST http://api:8000/write-application with body mapping job_id, resume_name from step 6, cover_letter from step 10.
    12. HTTP Request node: GET http://api:8000/resume-file/{resume_name} to fetch resume binary for email attachment.
    13. Gmail Send node (or Send Email SMTP node): per D-21. To: the apply email address from job.apply_type field or a dedicated apply_email field. Subject line: "Application for {job_title} -- Stefano D'Silva". Body: the generated cover letter text. Attachment: resume file binary from step 12. Use Gmail OAuth2 credential in n8n.
    14. HTTP Request node: POST http://api:8000/mark-submitted with body mapping job_id.
    15. Telegram node: per D-24. Message including company, job_title, resume_name, and job URL. If screening answers were generated, also mention "Screening answers provided". Use Telegram credential.

    On any HTTP 503 (challenge_detected) from FastAPI endpoints: branch to Telegram alert per OPS-01 instead of continuing.
    On any error in steps 9-13: branch to error handling Telegram alert and do NOT call /mark-submitted (job stays in APPLYING state for retry).

    Create n8n/README.md with setup and import documentation. Include these sections:
    - Prerequisites: Docker Compose running, API container healthy, Gmail OAuth token obtained via scripts/gmail_oauth.py, resume files placed in resumes/ directory, config/profile.yaml edited with real profile data
    - Credential setup in n8n UI: Anthropic API key as Header Auth (name "anthropicApi", header "x-api-key"), Telegram bot token, Gmail OAuth2 for email sending
    - Workflow import order: import error-handler first (so other workflows can reference it), then heartbeat, then gmail-ingest, jobspy-scrape, kalibrr-scrape, and finally ai-apply-pipeline
    - Configuration notes: how to edit Schedule Trigger intervals, how to change JobSpy search terms, profile data is sourced from config/profile.yaml via GET /profile (no need to edit n8n workflow when profile changes)
    - Troubleshooting: ECONNREFUSED means wrong URL (use http://api:8000 not localhost), credential errors mean re-enter in n8n UI, zero results from scrapers means possible IP block
    - Environment variables reference: list all env vars from .env.example with descriptions
  </action>
  <verify>
    <automated>cd /Users/stefano/Documents/Workspaces/Job Application Automation && python -c "import json; wf = json.load(open('n8n/workflows/ai-apply-pipeline.json')); assert 'nodes' in wf; nodes_str = json.dumps(wf); assert 'profile' in nodes_str.lower(); assert 'generate-screening-answers' in nodes_str or 'screening' in nodes_str; assert len(wf['nodes']) >= 7; print('ai-apply-pipeline.json is valid with', len(wf['nodes']), 'nodes, has profile fetch and screening answers')" && test -f n8n/README.md && echo "README exists"</automated>
  </verify>
  <acceptance_criteria>
    - n8n/workflows/ai-apply-pipeline.json is valid JSON with Schedule Trigger, GET /profile call at pipeline start, /queued-email-jobs poll, /select-resume call, conditional /generate-screening-answers call, Claude Sonnet cover letter generation using profile from /profile, /write-application, Gmail Send, /mark-submitted, Telegram notification
    - Workflow calls GET http://api:8000/profile once per run and uses returned profile data in cover letter prompt (NOT a static block)
    - Workflow conditionally calls POST http://api:8000/generate-screening-answers when job has non-empty screening_questions (per D-20, AI-03)
    - Workflow uses http://api:8000 for all FastAPI calls
    - Claude Sonnet call uses model "claude-sonnet-4-5" with a cover letter system prompt and profile fields from /profile response
    - Email node sends cover letter as body and resume as binary attachment per D-21
    - Telegram node sends company, role, resume_name, job_url per D-24
    - HTTP 503 responses from any FastAPI call branch to Telegram alert (OPS-01)
    - Errors in cover letter generation or email send do NOT call /mark-submitted
    - n8n/README.md contains prerequisite list, credential setup, import order, configuration notes (mentions profile.yaml as source of truth), troubleshooting, and env var reference
  </acceptance_criteria>
  <done>AI apply pipeline workflow orchestrates the full application flow from profile fetch through job polling, resume selection, conditional screening answer generation, cover letter generation with dynamic profile data, email submission, to Telegram notification; README documents complete setup process</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete n8n + FastAPI autonomous pipeline. Six n8n workflow JSON files ready for import. Docker Compose config for co-located deployment. All FastAPI endpoints operational including GET /profile, POST /generate-screening-answers.</what-built>
  <how-to-verify>
    1. Start the stack: docker compose up -d (from project root)
    2. Verify both containers are healthy: docker compose ps (both should show "Up")
    3. Open n8n UI: http://localhost:5678
    4. Import workflows: Workflows -> Import from File -> select each JSON from n8n/workflows/ (start with error-handler.json, then heartbeat.json, then the rest)
    5. Configure credentials in n8n UI:
       - Anthropic: Credentials -> Add -> Header Auth -> name "anthropicApi" -> Header Name "x-api-key" -> Value: your API key
       - Telegram: Credentials -> Add -> Telegram -> paste bot token
       - Gmail: Credentials -> Add -> Gmail OAuth2 -> complete OAuth flow
    6. Activate the heartbeat workflow -> verify Telegram receives "Heartbeat OK" within 30 minutes
    7. Manually trigger the jobspy-scrape workflow -> verify it calls /scrape-jobspy and processes results through /ingest-lead
    8. Verify profile endpoint: curl http://localhost:8000/profile -> should return JSON with summary, target_roles, etc.
    9. Check the FastAPI logs: docker compose logs api -- verify structlog JSON entries for ingestion, dedup, and filter events
    10. (Optional) Manually trigger gmail-ingest if Gmail OAuth is configured -> verify it polls and processes emails with apply_type="email"
    11. (Optional) Manually trigger ai-apply-pipeline with a test QUEUED job with apply_type="email" -> verify full flow including profile fetch, screening answer generation (if questions present), and cover letter generation to email send
  </how-to-verify>
  <resume-signal>Type "approved" if the end-to-end pipeline works (at minimum: heartbeat + one ingestion workflow + /ingest-lead processing visible in logs + GET /profile returns data), or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n -> Anthropic API | API key transmitted via HTTPS; response contains AI-generated content |
| n8n -> Gmail Send | OAuth2 token used to send emails as Stefano; high-value action |
| n8n -> Telegram API | Bot token used to send notifications; read access to chat |
| n8n UI -> browser | n8n web editor exposes workflow configuration and credentials |
| n8n -> GET /profile | Profile data fetched at pipeline start; contains personal career info |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Spoofing | n8n Gmail Send | mitigate | OAuth2 credential in n8n; email sent as authenticated Stefano; n8n encrypts credentials with N8N_ENCRYPTION_KEY |
| T-02-18 | Information Disclosure | n8n credentials store | mitigate | N8N_ENCRYPTION_KEY set once before first run; never regenerated; backed up alongside n8n_storage volume |
| T-02-19 | Tampering | n8n workflow execution | mitigate | n8n port 5678 bound to localhost or VPN only in production; no public exposure |
| T-02-20 | Denial of Service | Anthropic API rate limits | accept | Claude API has generous rate limits for single-user; at 50 apps/day with Sonnet calls, well under limits |
| T-02-21 | Information Disclosure | Cover letter content in Telegram | accept | Telegram message contains company/role/URL -- no PII beyond what Stefano already knows; private bot chat |
| T-02-22 | Repudiation | Email application sent | mitigate | /mark-submitted creates Application row + audit trail before Telegram fires; email is in Gmail Sent folder |
| T-02-SC | Tampering | n8n Docker image | mitigate | Official n8n image from docker.n8n.io; SHA pinning recommended for production but acceptable for MVP |
</threat_model>

<verification>
1. All 6 workflow JSON files are valid JSON and importable into n8n
2. n8n/README.md contains complete setup instructions
3. docker compose up starts both n8n and api containers
4. Heartbeat workflow sends Telegram ping
5. Ingestion workflows call correct FastAPI endpoints on http://api:8000
6. gmail-ingest.json sets apply_type="email" in /ingest-lead request body
7. ai-apply-pipeline calls GET /profile at pipeline start and uses profile data in Sonnet prompt
8. ai-apply-pipeline conditionally calls /generate-screening-answers when screening_questions is non-empty
9. ai-apply-pipeline generates cover letter, sends email, fires Telegram notification
10. Error handler catches workflow failures and alerts via Telegram
11. OPS-01: 503 responses from FastAPI trigger Telegram alerts in n8n
</verification>

<success_criteria>
The full autonomous pipeline is operational: n8n schedules Gmail polling (hourly, setting apply_type='email'), JobSpy/Kalibrr scraping (4-hourly), processes leads through filter/dedup, fetches profile data from GET /profile, selects resumes via Claude Haiku, generates screening answers when questions exist (AI-03), generates cover letters via Claude Sonnet using dynamic profile data (D-19), sends application emails, updates job status, and fires Telegram notifications -- all without human intervention. Error handling alerts on failures, heartbeat confirms the system is alive.
</success_criteria>

<output>
Create `.planning/phases/02-ingest-generate-and-email-apply/02-05-SUMMARY.md` when done
</output>
