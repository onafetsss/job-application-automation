---
context: phase
phase: 02-ingest-generate-and-email-apply
task: 0
total_tasks: 3
status: in_progress
last_updated: 2026-05-27T18:10:58.692Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints identified this session — execution went smoothly._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Route prefix drift | Executors added APIRouter prefixes (/gmail/, /scrape/, /resume/, /application/, /profile/) that differ from the un-prefixed URLs in plan 02-05's task spec | advisory | When implementing 02-05 n8n workflows, verify actual route paths with `uv run python -c "from src.api.app import app; [print(r.path) for r in app.routes]"` before writing workflow JSON |
| ROADMAP/STATE wipe on merge | Worktree agents commit empty STATE.md/ROADMAP.md; merge overwrites main's tracking files | advisory | After each wave merge, restore tracking files from pre-merge HEAD~1 and amend. Already handled — this is a known pattern to watch for |

<current_state>
Phase 02, Wave 2 complete. All code merged to main branch. Paused at two human-verify checkpoints before Wave 3 can start.

Plans completed and merged:
- 02-01 (FastAPI foundation) — fully done ✓
- 02-02 (Gmail ingestion) — code done, awaiting Gmail OAuth verification ⏸
- 02-03 (Scraper vertical slice) — code done, awaiting Kalibrr selector verification ⏸
- 02-04 (AI prep + application lifecycle) — fully done ✓

Plan remaining:
- 02-05 (n8n workflows) — not started, depends on 02-02 + 02-03 approval
</current_state>

<completed_work>

**Wave 1 — Plan 02-01:**
- FastAPI app with lifespan (init_db, session factory, eligibility config, API key auth on app.state)
- 14 Pydantic schemas (LeadIn/Out, Scrape*In, SelectResume*In/Out, WriteApplicationIn, MarkSubmittedIn, PollGmailOut, FetchEmailBody*, GenerateScreeningAnswers*, ProfileOut)
- POST /ingest/ingest-lead — full dedup + eligibility + audit pipeline, 4 integration tests pass
- AgentConfig model (agent_config table), 4 new AuditEvent values (APPLYING, SUBMITTED, FAILED, NOTIFIED)
- Docker Compose (n8n + api on agent-network, n8n bound to localhost for T-02-04)
- Dockerfile.api, config/profile.yaml placeholder, 5 stub route files

**Wave 2 — Plans 02-02, 02-03, 02-04 (all merged):**

02-02 (Gmail):
- src/ingestion/gmail_client.py — get_gmail_service(), poll_gmail_since(), fetch_message_body()
- src/api/routes/gmail.py — POST /gmail/poll-gmail, POST /gmail/fetch-email-body
- scripts/gmail_oauth.py — one-time OAuth token acquisition
- 5 unit tests + 4 integration tests pass (all mocked)

02-03 (Scrapers):
- src/ingestion/jobspy_runner.py — run_jobspy() async thread-executor wrapper
- src/ingestion/kalibrr_scraper.py — scrape_kalibrr() httpx+BS4 with 3-strategy CSS fallback
- src/api/routes/scrape.py — POST /scrape/scrape-jobspy, POST /scrape/scrape-kalibrr (OPS-01 zero-result detection)
- 5 integration tests pass

02-04 (AI prep + lifecycle):
- src/preparation/resume_reader.py, src/preparation/profile_loader.py
- src/api/routes/resume.py — POST /resume/select-resume (Claude Haiku), GET /resume/resume-file/{name}
- src/api/routes/profile.py — GET /profile/profile
- src/api/routes/application.py — POST /application/generate-screening-answers, POST /application/write-application, POST /application/mark-submitted, GET /application/queued-email-jobs
- src/api/app.py updated to load resumes_dir onto app.state
- 14 integration tests pass (3 resume + 2 profile + 4 screening + 5 application)
</completed_work>

<remaining_work>

**Human checkpoints (must be resolved before Wave 3):**

1. **02-02 Gmail OAuth** — Create GCP project + OAuth credentials, run scripts/gmail_oauth.py, verify /gmail/poll-gmail returns `{"message_ids": [...], "history_id": "..."}`. Steps in plan 02-02 checkpoint task.

2. **02-03 Kalibrr selectors** — Start server, curl POST /scrape/scrape-kalibrr with search_term="Product Manager". If count==0, inspect live HTML at kalibrr.com and report CSS structure so kalibrr_scraper.py can be patched.

**Wave 3 — Plan 02-05 (not started):**
- 6 n8n workflow JSON files: gmail-ingest.json, jobspy-scrape.json, kalibrr-scrape.json, ai-apply-pipeline.json, error-handler.json, heartbeat.json
- GET /application/queued-email-jobs + GET /resume/resume-file/{name} already implemented in 02-04 (plan 02-05 lists these as tasks — they're done)
- n8n/README.md
- IMPORTANT: All n8n HTTP Request nodes must use prefixed routes (see route prefix drift anti-pattern above)
- Ends with a blocking human-verify checkpoint: Docker Compose up + n8n import + heartbeat + one ingestion workflow end-to-end test
</remaining_work>

<decisions_made>

- Route prefixes added by executors: /gmail/, /scrape/, /resume/, /application/, /profile/ — plan 02-05 task spec uses un-prefixed paths; executors must use actual registered paths
- API key auth (X-API-Key header + verify_api_key dependency) added to all endpoints — n8n workflows need to include this header
- n8n bound to localhost:5678 only (not 0.0.0.0) for T-02-04 threat mitigation
- RESUMES_DIR env var loaded onto app.state in 02-04 — Dockerfile.api mounts ./resumes:/app/resumes
</decisions_made>

<blockers>
- None technical. Only human-action checkpoints blocking Wave 3.
</blockers>

## Required Reading (in order)
1. `.planning/phases/02-ingest-generate-and-email-apply/02-02-SUMMARY.md` — Gmail ingestion, OAuth setup instructions
2. `.planning/phases/02-ingest-generate-and-email-apply/02-03-SUMMARY.md` — Scraper endpoints, Kalibrr checkpoint details
3. `.planning/phases/02-ingest-generate-and-email-apply/02-04-SUMMARY.md` — AI prep, application lifecycle endpoints (already provides GET /queued-email-jobs and GET /resume-file)
4. `src/api/app.py` — Verify actual route prefixes registered before writing n8n workflow JSONs

## Infrastructure State
- All code merged to `main` branch — working tree is clean
- No running servers (stopped after session)
- .venv uses Python 3.14 — `uv run` fails due to numpy/Python 3.14 incompatibility; use `.venv/bin/python -m pytest` directly for testing
- Docker Compose not yet started (will be needed for 02-05 checkpoint verification)

<context>
This session ran execute-phase on Phase 02. Waves 1 and 2 executed without issues. All worktrees merged cleanly. The numpy build failure on Python 3.14 is pre-existing (test_dry_run_pipeline.py uses `uv run python main.py` which fails). Core tests all pass via `.venv/bin/python -m pytest`.

Wave 3 is one plan (02-05: n8n workflows) and is ready to go once the two human checkpoints are cleared. The plan is straightforward — generate 6 JSON files and a README. The main gotcha is the route prefix drift: executors registered routes with prefixes but the plan spec has un-prefixed URLs.
</context>

<next_action>
1. Perform Gmail OAuth checkpoint: create GCP credentials, run `scripts/gmail_oauth.py`, approve 02-02
2. Perform Kalibrr selector checkpoint: start server, curl /scrape/scrape-kalibrr, approve or fix and approve 02-03
3. Resume: `/gsd-execute-phase` — it will detect 02-05 as the only remaining plan and execute Wave 3
</next_action>
